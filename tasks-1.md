# Тестовые задания: Теория вероятности и логика

- [Задание 1. Фермер](#задание-1-фермер)
- [Задание 2. Кулинарное соревнование](#задание-2-кулинарное-соревнование)
- [Задание 3. Одинокая дорога](#задание-3-одинокая-дорога)

## Задание 1. Фермер

**На ферме содержатся шесть разных видов животных, и каждый раз, когда фермер заходит в сарай, он видит одно случайное животное. За день фермер заходит в сарай 6 раз. Каково математическое ожидание количества разных видов животных, которые фермер увидит за день?**

> Ответ округлить до сотых, например: `4,12`

**Ответ:** `3,99`

---

### Объяснение

> Самое сложное в теории вероятностей — это понять задачу.  
> А самое приятное — представлять, как животные танцуют в хороводе в сарае.

Давайте разберёмся по шагам.

#### 🧩 Что мы знаем?

- На ферме живут **животные 6 разных видов**.  
  Пусть это будут: корова, свинья, коза, баран, лошадь и гусь.  
  Главное — они разные.

- Каждый раз, когда фермер заходит в сарай, он **случайно встречает одного из них**.  
  Представим, что звери свободно бродят по сараю (или даже водят хоровод у входа).  
  И — очень важно — **каждый вид имеет одинаковый шанс быть увиденным**:  
  вероятность встретить любого — **1/6**.

- Фермер заходит в сарай **6 раз за день**.  
  Может кормит, может обнимает, а может просто прячется от вредной жены.  
  Каждый заход — независимое событие.


#### ❓ Что нужно найти?

Нам нужно вычислить **математическое ожидание** количества **разных видов животных**, которых фермер увидит за день.

🔹 Что такое *математическое ожидание*?  
Это **среднее значение**, которое получится, если повторять эксперимент много-много раз.

Представим, что мы наблюдали за фермером целый год, каждый день записывали, сколько **уникальных видов** он увидел, и потом посчитали среднее.

Примеры:
- В понедельник ему 6 раз попалась **корова** → увидел **1 вид**.
- Во вторник повезло: каждый раз — новый зверь → **6 видов**.
- В среду: баран, свинья, баран, гусь, свинья, гусь → **3 вида**.

Если бы мы взяли среднее по этим трём дням:  
`(1 + 6 + 3) / 3 = 10 / 3 ≈ 3,33`

Но это всего 3 дня!  
Чтобы получить точный ответ, не нужно наблюдать 100 лет — можно использовать **умную математику**.

#### 🔍 Хитрый способ: считаем "вклад" каждого вида

Вместо перебора всех комбинаций, воспользуемся приёмом:  
для каждого вида посчитаем, **насколько вероятно**, что фермер его **увидит хотя бы раз**.

Представим, что каждый вид "может добавить 1 к счёту", если его увидели.

Обозначим:
- X1 = 1, если увидел корову, иначе 0
- X2 = 1, если увидел свинью, иначе 0
- X3 = 1, если увидел козу, иначе 0
- X4 = 1, если увидел барана, иначе 0
- X5 = 1, если увидел лошадь, иначе 0
- X6 = 1, если увидел гуся, иначе 0

Тогда общее количество разных видов:
```
X = X1 + X2 + X3 + X4 + X5 + X6
```

Мы хотим найти среднее значение X — то есть E[X].

#### ✨ Главная идея: матожидание суммы = сумме матожиданий

Даже если события зависят друг от друга (например, увидев корову, нельзя увидеть свинью в тот же момент),  
**среднее от суммы = сумма средних**.

Поэтому:
```
E[X] = E[X1] + E[X2] + E[X3] + E[X4] + E[X5] + E[X6]
```

А что такое E[Xi]?  
Это вероятность того, что i-й вид был увиден хотя бы раз.

Например:
```
E[X1] = 1 × P(увидел корову) + 0 × P(не увидел) = P(увидел корову)
```

Значит, каждое матожидание — это просто **вероятность увидеть этот вид минимум один раз за 6 заходов**.

#### 🎯 Считаем вероятность увидеть один вид хотя бы раз

Возьмём корову.

- Вероятность увидеть корову за один заход: **1/6**
- Вероятность НЕ увидеть её: **5/6**
- Вероятность НЕ увидеть ни разу за 6 заходов:  
  `(5/6)^6 = (5/6) × (5/6) × (5/6) × (5/6) × (5/6) × (5/6) ≈ 0,3349`
- Значит, вероятность увидеть корову **хотя бы раз**:  
  `1 - 0,3349 = 0,6651`

То же самое — для всех 6 видов (все равны).

#### 📊 Складываем всё вместе

Каждый вид "вносит" в среднем около **0,6651** в итоговое число.

Сумма по 6 видам:
```
6 × 0,6651 = 3,9906
```

Округляем до **двух знаков после запятой**:
```
3,99
```

### 💡 Почему не 6?

Хотя фермер заходит 6 раз, он может несколько раз увидеть одного и того же зверя.  
Значит, некоторые виды остаются "невидимыми".  
Поэтому в среднем он видит **меньше 4 видов**, хотя заходов — 6.

---

## Задание 2. Кулинарное соревнование

**В конкурсе участвуют 80 шеф-поваров с уникальными уровнями мастерства. В первом этапе судьи случайным образом распределяют их по парам (в любом состязании двух шефов выигрывает тот, у кого выше уровень мастерства). На втором этапе шефы снова случайно образуют пары для финального раунда (пары могут повториться). Победную награду получают те, кто выиграл в обоих этапах. Каково математическое ожидание числа победителей?**

> Ответ округлить до десятых, например: `33,5`

**Ответ:** `26,8`

---

### Объяснение

> Какова вероятность встретить за углом динозавра?\
> Кто-то скажет: «50-на-50 — либо встречу, либо нет».\
> Так вот, это не так.


#### 🧩 Что мы знаем?

- У нас **80 шеф-поваров**, каждый со своим **уникальным уровнем мастерства**.
  - Один — гений, который может определить рецепт по запаху дыма.
  - Другой — подмастерье, который считает, что "слишком много чеснока" — это миф.
  - Но все начинают на равных.

- Соревнование проходит в **два этапа**:
  1. **Первый этап**: всех случайно разбивают на 40 пар. В каждой паре побеждает **тот, кто сильнее**.
  2. **Второй этап**: **все 80 человек снова случайно попариваются** — как будто первый раунд стёрся из памяти. Пары могут повториться.
     - Это **не финал между победителями**, а просто **ещё один независимый раунд**.

- Главный приз получают только те, кто **выиграл в обоих раундах**.

Наша цель — найти:  
> **Сколько шефов в среднем выиграют оба раза?**

То есть — каково **математическое ожидание числа дважды-победивших**?

#### ❓ Что такое "матожидание" здесь?

Представим, что мы провели это соревнование **много раз** — 1000 дней подряд.

Каждый день записываем, сколько шефов выиграли оба раунда.  
Потом считаем **среднее значение** — это и будет ответ.

Но ждать 1000 дней не надо — есть способ посчитать **здесь и сейчас**.

#### 🔍 Хитрый способ: считаем "вклад" каждого повара

Пусть каждый шеф может "принести 1 очко", если он выиграет **оба раунда**.

Обозначим:
- X0 = 1, если шеф №0 выиграл оба раза, иначе 0
- X1 = 1, если шеф №1 выиграл оба раза, иначе 0
- ...
- X79 = 1, если шеф №79 выиграл оба раза, иначе 0

Тогда общее число победителей:
```
X = X0 + X1 + X2 + ... + X79
```

Мы хотим найти среднее значение X → E[X]

#### ✨ Матожидание суммы = сумме матожиданий

Одно из самых полезных правил в теории вероятностей:

> **Среднее от суммы = сумме средних**

Даже если события зависят друг от друга!

Поэтому:
```
E[X] = E[X0] + E[X1] + ... + E[X79]
```

А что такое E[Xi]?  
Это **вероятность**, что i-й шеф выиграет **оба раунда**.

Значит, нужно для каждого шефа посчитать эту вероятность и всё сложить.

#### 🎯 Нумерация по силе

Чтобы было проще, пронумеруем шефов **по уровню мастерства**:

- Шеф №0 — самый сильный (ни у кого нет шансов против него)
- Шеф №1 — чуть слабее
- ...
- Шеф №79 — самый слабый (проиграет любому)

Теперь — какова вероятность, что шеф с номером **k** выиграет **один** раунд?

Он играет против **случайного соперника** из 79 других.

Он **выиграет**, если его соперник имеет номер **больше k** (то есть соперник слабее).

Сколько таких соперников?
`79 - k`

Например:
- У самого сильного (k=0): 79 слабее → шанс победить очень высокий
- У самого слабого (k=79): 0 слабее → шанс = 0

При случайной раздаче пар, вероятность победить в одном раунде примерно:
```
P(победа) = (79 - k) / 79
```

#### 🔄 Два независимых раунда

Раунды независимы — пары пересоздаются заново.

Значит, вероятность выиграть **оба** раунда:
```
P(выиграл оба) = [(79 - k) / 79] × [(79 - k) / 79] = [(79 - k) / 79]^2
```

То есть — **квадрат** вероятности выиграть один раунд.

#### 📊 Складываем "вклады" всех шефов

Теперь суммируем эти вероятности по всем 80 шефам:

```
E[X] = [79/79]^2 + [78/79]^2 + [77/79]^2 + ... + [1/79]^2 + [0/79]^2
```

Выносим знаменатель:
```
E[X] = (1 / 79^2) × (79² + 78² + 77² + ... + 1² + 0²)
```

Сумма квадратов чисел от 1 до n вычисляется по формуле:
```
Сумма = n × (n + 1) × (2n + 1) / 6
```

У нас сумма от 1 до 79:
- n = 79
- n+1 = 80
- 2n+1 = 159

Считаем сумму квадратов:
```
79 × 80 × 159 / 6 = 167480
```

Считаем матожидание:
```
167480 / 79^2 = 26,8354
```

Округляем до **десятых**:
```
26,8
```

### 💡 Почему не 20?

Может показаться, что:
- Каждый имеет ~50 % шанс выиграть один раунд
- Тогда шанс выиграть два раза: 0,5 × 0,5 = 0,25
- 80 × 0,25 = 20

Но это **неверно**, потому что:
- **Сильные шефы почти всегда выигрывают** (их шанс близок к 100%)
- **Слабые почти всегда проигрывают**
- А "середнячки" — выигрывают, если им попадутся ещё более слабые!

Перекос матожидания в сторону сильных поваров происходит, потому что:
> Cреднее квадратов больше квадрата средних. Это называется неравенство Йенсена.\
> Победы сильных поваров "натягивают" матожидание вверх на себя.

Поэтому результат — **не 20, а 26,8**.

---

## Задание 3. Одинокая дорога

**На пустынном шоссе вероятность появления автомобиля за 30-минутный период составляет 0,95.  
Какова вероятность его появления за 10 минут? А за 27 минут?**

> Ответ дать в процентах, округлив до десятых через точку с запятой, например: `42,7; 95,0`

**Ответ:** `63,2; 93,3`

---

### Объяснение

> Дорога длинная. Песок, жара, одинокий столб.  
> Ты стоишь с пустым баком и думаешь: «Когда же наконец появится машина?»  
> Эта задача — про надежду… и немного про математику.

#### 🧩 Что мы знаем?

- На шоссе **очень редко** появляются автомобили.
- За **30 минут** вероятность увидеть **хотя бы одну машину** — **0,95**, то есть **95%**.
- Нам нужно найти:
  - Вероятность увидеть хотя бы одну машину за **10 минут**
  - И за **27 минут**

#### 🔍 Как такое вообще считать?

Есть хороший способ — **модель случайных событий во времени**, которую часто используют для таких ситуаций:  
> **Пуассоновский процесс**.

🔹 Идея простая:
- Машины появляются **случайно и независимо** друг от друга.
- Есть **средняя интенсивность** — сколько машин в среднем приходит за минуту.
- И тогда можно посчитать вероятность увидеть **хотя бы одну машину** за любой промежуток.

#### ✨ Ключевая формула

Вероятность того, что **за время t не появится ни одной машины**, равна:

```
P(никакой машины за t) = e^(-λt)
```

где:
- λ — среднее количество машин в минуту (интенсивность),
- t — время в минутах,
- e ≈ 2,718.

Тогда вероятность увидеть **хотя бы одну машину**:
```
P(хотя бы одна) = 1 - e^(-λt)
```

#### Шаг 1: Найдём λ (интенсивность)

Из условия:
- За 30 минут P(увидеть хотя бы одну) = 0,95
- Значит, P(не увидеть ни одной за 30 мин) = 1 - 0,95 = 0,05

Подставляем в формулу:
```
e^(-λ × 30) = 0,05
```

Теперь "разворачиваем" это уравнение.

Берём натуральный логарифм (ln) от обеих сторон (это называется "пропотенциировать"):
```
-λ × 30 = ln(0,05)
```

ln(0,05) ≈ -2,9957

Значит:
```
λ × 30 = 2,9957
λ = 2,9957 / 30 ≈ 0,09986
```

То есть примерно **0,1 машины в минуту** (по интенсивности).

#### Шаг 2: Считаем за 10 минут

t = 10

P(не увидеть ни одной) = e^(-λ × 10) = e^(-0,09986 × 10) = e^(-0,9986) ≈ 0,3685

Тогда:
```
P(увидеть хотя бы одну) = 1 - 0,3685 = 0,6315
```

Переводим в проценты: **63,15%** → округляем до десятых: **63,2%**

#### Шаг 3: Считаем за 27 минут

t = 27

P(не увидеть ни одной) = e^(-λ × 27) = e^(-0,09986 × 27) ≈ e^(-2,696) ≈ 0,0674

Тогда:
```
P(увидеть хотя бы одну) = 1 - 0,0674 = 0,9326
```

В процентах: **93,26%** → округляем до десятых: **93,3%**

### 📊 Итог

- За **10 минут**: **63,2%**
- За **27 минут**: **93,3%**

### 💡 Почему не 95/3 = 31,7%?

Если бы кто-то сказал:  
> «30 минут — 95%, значит, 10 минут — это треть, будет 31,7%»,  

— он бы ошибся.

Потому что:
- Вероятность накапливается **не линейно**, а по экспоненте.
- За первые 10 минут шанс увидеть машину — 63%
- За следующие 10 — ещё 63% *из оставшихся*, и т.д.
- Поэтому за 30 минут получается уже 95%

Это как зарядка телефона: сначала быстро, потом всё медленнее.

> Дорога не так уж и одинока.  
> Просто машины приходят, когда их ждут…
